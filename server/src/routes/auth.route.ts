import { Router } from 'express';
const router = Router();
const auth = require("./../controller/auth.controller")

router
    // .route("/token")
    // .post(auth)
    // .get(auth)

module.exports = router