import { Router } from 'express';
const router = Router();
const product = require("./../controller/products.controller")

router
    // .route("/")
    // .post(product)
    // .get(product)

module.exports = router