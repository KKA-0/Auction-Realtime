import { Router } from 'express';
const router = Router();
const user = require("./../controller/user.controller")

router
    // .route("/")
    // .post(user)
    // .get(user)

module.exports = router